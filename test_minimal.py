#!/usr/bin/env python3\n"""Minimal test script for verifying environment setup and imports.\n\nThis script checks that all required dependencies are properly installed\nand can be imported without errors.\n"""\n\nprint(\"Starting...\")\n\nimport os\nimport sys\n\nprint(\"Imports done\")\n\n# Add the backend directory to the Python path so local modules can be imported\nsys.path.insert(0